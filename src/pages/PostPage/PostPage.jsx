import './PostPage.css';


export default function PostPage({ user, alterUser, handleError }) {

    return (
        <main className='PostPage'>
            <div>PostPage</div>
        </main>
    );
}